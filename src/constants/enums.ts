export enum EAdvertisementStatus {
  PENDING_APPROVAL = "pending_approval",
  APPROVED = "approved",
  REJECTED = "rejected",
}

export enum EManagementTab {
  SETTING = "setting",
  BRAND = "brand",
  TABLE = "table",
  EMPLOYEE = "employee",
  DEVICE = "device",
}
