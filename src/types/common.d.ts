type TModalRef = {
  showModal: () => void;
  closeModal: () => void;
};

type TDrawerRef = {
  showDrawer: () => void;
  closeDrawer: () => void;
};

type TAcionDrawer = {
  onClose: () => void;
  onOpen: () => void;
};
